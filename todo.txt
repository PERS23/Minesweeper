todo:

    - Stop button click animation for flagged tiles
    - Add help section
    - Add so if mouse is not on cell on release it won't trigger
    - Test number of uncovered tiles reports correctly

done:

    - Load fonts from file
    - Add resize limits
    - Fix bug where when all are marked, it doesn't detect victory
        * I think, added line where click doesn't do anything unless it is covered
    - Difficulty change hot-keys
    - Implement timer functionality
    - Add statistics
    - Format statistics better
    - Make statistics serializable
    - Create and use a string file